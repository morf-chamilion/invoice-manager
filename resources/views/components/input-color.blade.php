<input type="color" name="{{ $name }}" id="{{ $id }}"
    @if (!empty($value)) value="{{ $value }}" @endif class="form-control" style="height: 45px;">
<p class="text-muted mt-2">Select a color from the palette.</p>
